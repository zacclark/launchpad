<div class="widget settings">
  <div class="header">
    <h2>Widget Settings</h2>
    <%= link_to "Edit", "#", :class => "button left", :id => "settings_edit" %>
    <%= link_to "✚", new_widget_path, :class => "button right" %>
  </div>
  
  <div id="my_widgets">
    <% @widgets.each do |widget| %>
      <div class="section common">
        <h3><%= widget.class.title %></h3>
        <p><%= button_to "Delete", widget, :method => :delete, :class => "delete button hidden" %></p>
      </div>
    <% end %>
  </div>
</div>